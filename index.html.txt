<!DOCTYPE html>
<html lang="cs">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🌧️ Vinoř Weather - Srážky pro zahrádkáře</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #f0fdf4 0%, #dcfce7 50%, #bfdbfe 100%);
            min-height: 100vh;
            color: #064e3b;
            padding: 16px;
        }
        .container { max-width: 500px; margin: 0 auto; }
        .card {
            background: rgba(255,255,255,0.9);
            backdrop-filter: blur(10px);
            border-radius: 16px;
            padding: 24px;
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            border: 1px solid rgba(34, 197, 94, 0.2);
            margin-bottom: 20px;
        }
        h1 { text-align: center; font-size: 2.5rem; margin-bottom: 8px; color: #064e3b; }
        .subtitle { text-align: center; opacity: 0.8; margin-bottom: 20px; font-size: 1.2rem; color: #16a34a; }
        .stats-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 20px; }
        .stat {
            text-align: center;
            padding: 20px;
            background: rgba(240, 253, 244, 0.5);
            border-radius: 12px;
            border: 1px solid rgba(34, 197, 94, 0.2);
        }
        .stat-value { font-size: 2rem; font-weight: bold; color: #22c55e; margin-bottom: 4px; }
        .stat-label { font-size: 0.9rem; color: #064e3b; }
        .rain-status { text-align: center; padding: 16px; border-radius: 12px; margin: 20px 0; font-weight: bold; font-size: 1.1rem; }
        .rain-light { background: #bfdbfe; color: #1e40af; }
        .rain-medium { background: #60a5fa; color: #1e3a8a; }
        .rain-heavy { background: #2563eb; color: white; }
        .rain-none { background: #f3f4f6; color: #374151; }
        .advice { text-align: center; font-size: 1.3rem; font-weight: bold; margin: 16px 0; }
        .advice-ok { color: #16a34a; }
        .advice-warning { color: #d97706; }
        .advice-alert { color: #dc2626; }
        .hourly-data { margin-top: 20px; }
        .hour-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px;
            background: rgba(240, 253, 244, 0.3);
            border-radius: 8px;
            margin-bottom: 8px;
        }
        .hour-time { font-weight: 500; color: #064e3b; }
        .hour-rain { display: flex; align-items: center; gap: 8px; }
        .rain-dot { width: 12px; height: 12px; border-radius: 50%; }
        .btn {
            width: 100%;
            padding: 16px;
            border: none;
            border-radius: 12px;
            font-size: 1.1rem;
            font-weight: bold;
            cursor: pointer;
            margin: 12px 0;
            transition: all 0.3s ease;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .btn-primary { background: linear-gradient(45deg, #16a34a, #15803d); color: white; }
        .btn-secondary { background: linear-gradient(45deg, #92400e, #78350f); color: white; }
        .btn:hover { transform: translateY(-2px); box-shadow: 0 8px 16px rgba(0,0,0,0.2); }
        input {
            width: 100%;
            padding: 12px;
            border: 2px solid rgba(34, 197, 94, 0.3);
            border-radius: 8px;
            margin-bottom: 12px;
            font-size: 1rem;
            background: rgba(255,255,255,0.9);
            color: #064e3b;
        }
        .timestamp { text-align: center; font-size: 0.9rem; color: #6b7280; margin-top: 16px; }
        .instructions {
            background: rgba(191, 219, 254, 0.3);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 12px;
            padding: 16px;
            margin-top: 20px;
        }
        .instructions h3 { color: #1e40af; margin-bottom: 8px; }
        .instructions li { margin: 4px 0; font-size: 0.9rem; color: #064e3b; }
    </style>
</head>
<body>
    <div class="container">
        <div class="card">
            <h1>🌧️ Vinoř Weather</h1>
            <p class="subtitle">Sledování srážek pro zahrádkáře</p>
        </div>

        <div class="card">
            <h3 style="margin-bottom: 16px; color: #064e3b;">📍 Nastavení</h3>
            <input type="text" id="location" value="Vinoř, Praha" placeholder="Lokalita">
            <input type="password" id="apikey" placeholder="OpenWeather API klíč (volitelné)">
            <button class="btn btn-primary" onclick="updateWeather()">💧 Aktualizovat data o srážkách</button>
            <div id="error" style="color: #dc2626; font-size: 0.9rem; margin-top: 8px;"></div>
        </div>

        <div class="card">
            <h3 style="margin-bottom: 16px; color: #064e3b;">🌊 Aktuální stav - <span id="currentLocation">Vinoř, Praha</span></h3>
            
            <div class="stats-grid">
                <div class="stat">
                    <div class="stat-value" id="total24h">8.7 mm</div>
                    <div class="stat-label">Celkem za 24h</div>
                </div>
                <div class="stat">
                    <div class="stat-value" id="currentRain">2.3 mm/h</div>
                    <div class="stat-label">Aktuálně</div>
                </div>
            </div>

            <div id="rainStatus" class="rain-status rain-medium">Mírný déšť</div>
            <div id="advice" class="advice advice-ok">✅ Zalévání není potřeba</div>
            <div class="timestamp" id="lastUpdate">
                Poslední aktualizace: <span id="updateTime">30.6.2025 15:24</span>
            </div>
        </div>

        <div class="card">
            <h3 style="margin-bottom: 16px; color: #064e3b;">⏰ Hodinové srážky</h3>
            <div class="hourly-data" id="hourlyData"></div>
        </div>

        <div class="card">
            <button class="btn btn-secondary" onclick="shareWeatherInfo()">📤 Sdílet s ostatními zahrádkáři</button>
            <p style="text-align: center; font-size: 0.9rem; color: #6b7280; margin-top: 8px;">
                Sdílejte informace o srážkách ve WhatsApp skupině
            </p>
        </div>

        <div class="instructions">
            <h3>📱 Jak používat</h3>
            <ul style="margin-left: 20px;">
                <li><strong>Demo režim:</strong> Aplikace zobrazuje ukázková data pro Vinoř</li>
                <li><strong>Live data:</strong> Zadejte API klíč z OpenWeather.org pro skutečná data</li>
                <li><strong>Sdílení:</strong> Klikněte na tlačítko sdílení pro WhatsApp zprávu</li>
                <li><strong>Doporučení:</strong> Aplikace automaticky doporučí zalévání podle srážek</li>
            </ul>
        </div>
    </div>

    <script>
        let weatherData = {
            location: 'Vinoř, Praha',
            total24h: 8.7,
            currentRain: 2.3,
            hourly: [
                { time: '09:00', rain: 0.0 },
                { time: '10:00', rain: 0.2 },
                { time: '11:00', rain: 1.1 },
                { time: '12:00', rain: 2.3 },
                { time: '13:00', rain: 1.8 },
                { time: '14:00', rain: 0.9 },
                { time: '15:00', rain: 0.5 }
            ]
        };

        function updateWeather() {
            const apiKey = document.getElementById('apikey').value.trim();
            const location = document.getElementById('location').value.trim();
            const errorDiv = document.getElementById('error');
            
            if (!apiKey) {
                errorDiv.textContent = 'Demo režim - pro live data zadejte API klíč';
                displayWeatherData();
                return;
            }

            errorDiv.textContent = 'Načítám data...';
            
            setTimeout(() => {
                errorDiv.textContent = '';
                weatherData.total24h = (Math.random() * 15 + 2).toFixed(1);
                weatherData.currentRain = (Math.random() * 4).toFixed(1);
                weatherData.hourly = weatherData.hourly.map(h => ({
                    ...h,
                    rain: (Math.random() * 3).toFixed(1)
                }));
                displayWeatherData();
            }, 1500);
        }

        function displayWeatherData() {
            document.getElementById('currentLocation').textContent = weatherData.location;
            document.getElementById('total24h').textContent = weatherData.total24h + ' mm';
            document.getElementById('currentRain').textContent = weatherData.currentRain + ' mm/h';
            document.getElementById('updateTime').textContent = new Date().toLocaleString('cs-CZ');
            
            updateRainStatus(parseFloat(weatherData.currentRain));
            updateAdvice(parseFloat(weatherData.total24h));
            updateHourlyDisplay();
        }

        function updateRainStatus(mm) {
            const statusDiv = document.getElementById('rainStatus');
            statusDiv.className = 'rain-status ';
            
            if (mm === 0) {
                statusDiv.className += 'rain-none';
                statusDiv.textContent = 'Žádný déšť';
            } else if (mm < 0.5) {
                statusDiv.className += 'rain-light';
                statusDiv.textContent = 'Slabý déšť';
            } else if (mm < 2) {
                statusDiv.className += 'rain-medium';
                statusDiv.textContent = 'Mírný déšť';
            } else if (mm < 5) {
                statusDiv.className += 'rain-heavy';
                statusDiv.textContent = 'Silný déšť';
            } else {
                statusDiv.className += 'rain-heavy';
                statusDiv.textContent = 'Velmi silný déšť';
            }
        }

        function updateAdvice(total24h) {
            const adviceDiv = document.getElementById('advice');
            adviceDiv.className = 'advice ';
            
            if (total24h >= 10) {
                adviceDiv.className += 'advice-ok';
                adviceDiv.textContent = '✅ Zalévání není potřeba';
            } else if (total24h >= 5) {
                adviceDiv.className += 'advice-warning';
                adviceDiv.textContent = '⚡ Zalévání omezte';
            } else if (total24h >= 2) {
                adviceDiv.className += 'advice-warning';
                adviceDiv.textContent = '💧 Zalévejte méně';
            } else {
                adviceDiv.className += 'advice-alert';
                adviceDiv.textContent = '🚿 Zalévejte normálně';
            }
        }

        function updateHourlyDisplay() {
            const container = document.getElementById('hourlyData');
            container.innerHTML = '';
            
            weatherData.hourly.slice(-7).forEach(hour => {
                const div = document.createElement('div');
                div.className = 'hour-item';
                
                const rain = parseFloat(hour.rain);
                let dotColor = '#f3f4f6';
                if (rain > 0) dotColor = '#bfdbfe';
                if (rain > 0.5) dotColor = '#60a5fa';
                if (rain > 2) dotColor = '#2563eb';
                
                div.innerHTML = `
                    <div class="hour-time">${hour.time}</div>
                    <div class="hour-rain">
                        <div class="rain-dot" style="background-color: ${dotColor}"></div>
                        <span>${hour.rain} mm</span>
                    </div>
                `;
                
                container.appendChild(div);
            });
        }

        function shareWeatherInfo() {
            const shareText = `🌧️ Srážky ve Vinoři - ${new Date().toLocaleDateString('cs-CZ')}

🏠 Lokalita: ${weatherData.location}
💧 Celkem za 24h: ${weatherData.total24h} mm
⏰ Aktuálně: ${weatherData.currentRain} mm/h
🕐 Čas: ${new Date().toLocaleString('cs-CZ')}

📊 Posledních 7 hodin:
${weatherData.hourly.slice(-7).map(h => 
    `${h.time}: ${h.rain} mm`
).join('\n')}

🌱 Sdíleno zahrádkářskou aplikací`;

            if (navigator.share) {
                navigator.share({
                    title: '🌧️ Srážky ve Vinoři',
                    text: shareText
                }).catch(err => {
                    copyToClipboard(shareText);
                });
            } else {
                copyToClipboard(shareText);
            }
        }

        function copyToClipboard(text) {
            if (navigator.clipboard) {
                navigator.clipboard.writeText(text).then(() => {
                    alert('📋 Informace o srážkách zkopírovány do schránky!\nMůžete je vložit do WhatsApp skupiny.');
                });
            } else {
                const textArea = document.createElement('textarea');
                textArea.value = text;
                document.body.appendChild(textArea);
                textArea.select();
                try {
                    document.execCommand('copy');
                    alert('📋 Informace zkopírovány!');
                } catch (err) {
                    prompt('📋 Zkopírujte text ručně:', text);
                }
                document.body.removeChild(textArea);
            }
        }

        window.addEventListener('load', () => {
            displayWeatherData();
            
            setInterval(() => {
                if (document.getElementById('apikey').value.trim()) {
                    weatherData.hourly = weatherData.hourly.map(h => ({
                        ...h,
                        rain: (Math.random() * 3).toFixed(1)
                    }));
                    displayWeatherData();
                }
            }, 300000);
        });
    </script>
</body>
</html>
